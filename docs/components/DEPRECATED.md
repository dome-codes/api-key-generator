# Deprecated Components

Diese Komponenten wurden durch neue, strukturierte Komponenten ersetzt und sollten nicht mehr verwendet werden.

## Deprecated Components

- `UsageChart.vue` → Ersetzt durch `charts/LineChart.vue`
- `UsageAdditionalCharts.vue` → Ersetzt durch `charts/PieChart.vue` und `charts/BarChart.vue`
- `UsageFilters.vue` → Ersetzt durch `shared/BaseFilters.vue` + `ai/AIUsageFilters.vue`
- `UsageSummary.vue` → Ersetzt durch `shared/BaseSummary.vue` + `ai/AIUsageSummary.vue`

## Migration

Siehe `README.md` für Details zur Migration zu den neuen Komponenten.
